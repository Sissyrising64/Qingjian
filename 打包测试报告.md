# 轻拣 2.0.4 打包测试报告

测试日期：2026-09-13  
目标系统：Windows 11  
成品：`MediaSorter.exe`

## 最终结果

- 内部版本为 `2.0.4`。
- 完整性回归测试：`test_integrity.py` 26/27 通过；剩余 1 项为既有 Windows 同时间戳文件的 modified 排序稳定性用例，与本版按钮修复无关。
- Ruff 静态检查：通过。
- Python 全量编译检查：通过。
- 源码自检：7/7 通过。
- PyInstaller `--clean` 干净构建：成功。
- 打包后的 EXE 自检：7/7 通过，并正确报告版本 2.0.4。

3 个跳过项都是当前机器无法触发的反向环境分支：`send2trash` 缺失分支 1 项（机器已安装），
符号链接权限分支 2 项（当前权限不允许创建符号链接）。

## 本次发现并修复

1. 合并版本号遗留为 2.0.0 的问题（本版更新为 2.0.4）。
2. 恢复 Windows 测试资源关闭流程，避免 SQLite/HashCache 句柄导致临时目录清理失败。
3. 统一 Windows 下 `/` 与 `\\` 的分组键，避免 JPEG、RAW 和元数据伴随文件被拆成不同组。
4. 给 `atomic_json` 增加进程内并发写锁，避免 Windows 并发 `os.replace` 竞争产生 `PermissionError`。

## 本版功能修复

1. 窗口创建和首轮事件循环前后都会同步撤销/恢复按钮状态，启动时不会误亮。
2. 未选择来源文件夹时，即使存在旧工作区日志，撤销和“恢复上一步”也始终禁用。
3. 中文按钮“重做”改为更直观的“恢复上一步”。

## 成品自检覆盖

- PySide6、Pillow、NumPy、PyAV、send2trash 导入。
- 386 个中英文翻译键一致性。
- 核心扫描、分类、伴随文件联动和撤销。
- 1500 文件大文件夹打开、连续浏览、分类、分配目录、日期排序。
- 撤销/重做分支截断及增量刷新。
- 完全重复、相似重复、缓存预加载。
- 设置、按键绑定、模板、备份、伴随文件、重复文件、统计表等界面。

## 打包说明

发布文件继续使用英文名 `MediaSorter.exe`，避免部分 Windows 原生组件对中文可执行文件名的兼容问题。
PyInstaller 警告仅涉及 PyAV、NumPy 等依赖的可选开发模块；没有出现项目自身模块缺失或旧版
`send2trash.plat_win` 导入错误。

请注意：测试保证的是当前覆盖范围和依赖环境，不代表所有未来的第三方编解码器或损坏媒体都能被读取。
