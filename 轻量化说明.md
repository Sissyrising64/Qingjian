# 2.0.3：按键分配文件夹会重扫，以及一次全面的卡顿排查

## 一、你报的那个：分配文件夹后重扫目标文件夹

```python
if folder:
    binding.folder = folder
    self.engine.save_settings()
    self._refresh_bindings()
    if self.settings.recursive:
        self.rescan()          # ← 这一行
```

存在的理由是真的：递归扫描会跳过"目标文件夹"，所以如果你选的文件夹**在源文件夹下面**，那底下的文件得从队列里去掉。但用整棵树重走一遍来做这件事，代价和重新打开文件夹一样。

现在改成在内存里筛：

| | 之前 | 现在 |
|---|---|---|
| 目标文件夹在别处（正常情况） | 320 ms 重扫 | **0.3 ms，什么都不做** |
| 目标文件夹在源文件夹下面 | 320 ms 重扫 | **54 ms 内存筛选** |

第一版我写成对每个文件调 `resolve()`，2 万文件跑了 **1670 ms —— 比重扫还慢**。改成每个目录只解析一次、两个列表各重建一次（原来是逐个 `remove`，那是平方级的），才到 54 ms。

**顺带修掉一个能清空整个库的 bug**：文件选择对话框默认打开在源文件夹。如果你不导航直接点"选择文件夹"，目标就等于源文件夹本身，旧逻辑判定"整个源文件夹都要排除"——`all_files` 和队列全部清零，窗口显示"没有媒体文件"。而 `scan()` 其实只跳过**子**目录，从来不跳过根目录。现在两边对齐了。

---

## 二、全面排查：我把界面线程上每一个调用都测了一遍

2 万文件的文件夹，超过 20 ms 的都标出来。

### 按每次按键（必须接近 0）—— 全部合格

翻页、找配对文件、读标签、判断能不能撤销、读元数据，**全部在 0.05 ms 以下**。这条路径是干净的。

### 真正的问题：按日期排序，冷启动 11.8 秒

第一次对一个文件夹按日期排序：

- 每个文件单独查一次缓存 → 每次查都要 `stat` 一次来算缓存键
- 缓存没命中就走完整的元数据解析：把每一个 EXIF 标签都转成名字放进字典，然后**再整份拷贝一次**进 `raw_tags`
- 每个结果单独写一次数据库并 **commit**——2 万次磁盘刷写

三个都修了：

| | 之前 | 现在 |
|---|---|---|
| 首次按日期排序（2 万文件） | **11 774 ms** | **2 609 ms** |
| 之后每次 | 467 ms | **290 ms** |

- 新增 `capture_only()`：只按编号读那三个日期标签，不建标签名字典、不拷贝 `raw_tags`。逐张验证过它和完整解析给出**完全相同**的时间戳（125 张真实照片，0 个分歧）；RAW 和视频它返回 None，交回完整解析器。
- 缓存一次性批量读、一次性批量写。
- 排序拿到的是一个普通字典，不再逐个问缓存——那等于每个文件再 `stat` 一次。

顺带一提：我试过用线程池并行解析 EXIF，**反而更慢**（0.79×）。因为它 75% 的时间是 Python 在持有 GIL，不是等磁盘。所以这里明确用单线程，代码里写了原因。

### 队列重建：285 ms → 148 ms

每次改筛选、改排序、切预设都会走这条路。拆开看：

| | 之前 | 现在 | 怎么改的 |
|---|---|---|---|
| `collapse_groups`（按拍摄分组） | 159 ms | **32 ms** | 每个文件原来要构造 3 个 `Path` 对象 |
| `apply_filter` | 114 ms | **44 ms** | "全部"筛选走直通路径 |

分组键改成纯字符串运算了。这个改动有风险——这些字符串是**文件夹索引和配对文件分组的键**，答案变了就意味着 RAW 不跟着 JPEG 走。所以我用 322 个刁钻文件名（`a.b.c.jpg`、`.hidden`、`IMG.jpg.JPG`、`trailing.`、中日文名、Windows 路径…）逐个对拍旧实现，**0 个分歧**，并且把这个对拍留成了测试。有一处 pathlib 和 `os.path` 真的不一致（结尾是单个点的文件名），单独处理了。

### 大 PNG / TIFF 预览：卡 5.3 秒

JPEG 有 DCT 缩放，解码时就能按 1/8 出图。**PNG、TIFF、WebP 没有**——必须整张解开：

| | 磁盘大小 | 解码到 1400px | 峰值内存 |
|---|---|---|---|
| 6000×4000 JPEG | 6 MB | 73 ms | 4.5 MB |
| 6000×4000 PNG | 36 MB | 727 ms | 72 MB |
| 12000×9000 PNG | 117 MB | **5 275 ms** | **324 MB** |

**5.3 秒，在界面线程上。**现在超过阈值的非 JPEG 交给工作线程，界面先显示文件名和"正在读取这张大图…"，解完再换上去。JPEG 和 RAW 仍然同步解（它们本来就快），所以普通照片不会有任何闪烁。

### 其他几处

- **历史窗口**：2000 条 × 6 列 = 12 000 个单元格，一次性建完再逐格量宽度。改成分批填充 + 只按 60 行量宽度。
- **关闭窗口**：`wait_idle(30)` 会黑着脸卡最多半分钟。改成和撤销一样的排空，窗口继续刷新。
- **关掉"包含子文件夹"**：这只会减少文件，不需要重走目录树。
- **查重窗口关闭**：原来无条件重建队列（148 ms），现在只有真的往复查队列里放了东西才重建。
- **同步模式下每次分类**：原来每按一次键就整队列重建一次。改成增量。

---

## 三、子代理审查又翻出 13 个，其中两个是我这轮改出来的

我让一个子代理逆向审查这一轮的 Qt 改动。它找到的：

**严重（我这轮引入的）**：**超过 12 MB 的视频会永远卡在占位符上。** 我的"要不要交给工作线程"判断只看大小和格式，视频既不在"能边解码边缩放"的名单里也不是 RAW，所以判定为"慢"→ 显示占位符 → 但预取器开头就 `if is_video: return False`，任务根本没排队，信号永远不来。占位符永远不会被替换，播放器也拿不到文件。**任何一个正常大小的视频都会中招。**

第一次修的时候我把 `may_animate` 也排除了——结果 PNG 和 WebP 属于"可能是动图"，等于把最需要修的情况又漏出去了。最终规则：只排除视频；PNG/WebP 即使可能是动图也走异步，因为预览本来就先试动图分支，动图仍然会动，只是多解码一次。

**严重（我这轮引入的）**：**复制/收藏/跳过之后，那一行不会从队列里消失。** 这三个动作不移动文件，只改"已处理"名单，而我的增量逻辑看到文件还在原地就判定"没变化"。同步模式下按住键 = 同一张照片被复制无数次（`IMG_0001.jpg`、`IMG_0001 (1).jpg`…），队列长度纹丝不动。

还有：窗口在大图解码时被拉伸会让那张图**永久**显示失败（目标尺寸是缓存键的一部分）；新增的几个入口漏了忙碌保护；`_apply_change` 算出了增量补丁却又整条胶片条重建了一遍；重命名会把同一条记录 absorb 两次；被取消的网格构建再也不会被补完；进度回调每个文件调一次 = 2 万次事件循环；1970-01-01 拍摄时间被当成"没有值"。全部修了。

---

## 四、测试

**336 个**（上一版 303）。新增的每一条我都验证过"去掉修复它会失败"：

- 去掉视频例外 → 5 个失败
- 把 `absorb` 退回"文件还在就跳过" → 6 个失败
- 把"目标等于源文件夹"当成排除 → 1 个失败

其中两个测试是拿旧实现当基准的对拍：分组键的 322 个文件名，和 `capture_only` 对 125 张真实照片。

`--selfcheck` 的 large folder 阶段现在会打印分配文件夹和日期排序的耗时，并且会在第二次日期排序不比第一次快时直接判失败。

---

## 五、请你在 Windows 上确认

```
python -m PyInstaller --noconfirm qingjian.spec
MediaSorter.exe --selfcheck
```

`interface` 那项在我这里 FAIL（容器装不上 PySide6），你机器上应该 PASS。

手动五条：

1. **你报的那条**：给一个按键分配文件夹，应该**立刻**完成，不重扫。
2. 分配的时候不导航、直接点"选择文件夹"（也就是选中源文件夹本身），文件列表**不该被清空**。
3. 切到按日期排序：第一次会有进度条，之后每次都应该很快。
4. 打开一个**视频**，应该正常播放，不是停在"正在读取"。打开一张很大的 PNG/TIFF，应该先看到文件名再看到图，中间窗口不卡。
5. 把"后台执行分类操作"**关掉**，然后按住复制或收藏键——照片应该一张张往前走，不该反复复制同一张。
