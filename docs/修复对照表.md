# 轻拣 2.0 · 修复与新功能对照表

对照《功能建议与代码审查》里列出的每一条。每一项都标出**改在哪里**和**怎么验证**。

---

## 一、P0 · 影响实际可用性

### 1. 移动大文件有 3 倍以上 IO 放大 ✅

**原状**：同盘移动也走「快照复制 → 复制到目标 → 回读校验 → 删源」，移动 5 GB 视频约 15 GB 读写 + 5 GB 快照。

**改法**：`core/safestore.py` 的 `MOVE` 步骤在 `same_volume()` 为真时直接 `os.replace`，一次系统调用，不复制、不回读、不留快照；撤销由 `invert()` 生成反向 `MOVE`。跨卷才退回复制+校验+删源，且临时文件带事务 id、失败即清理。

> 哈希是用来证明**副本**忠实的；改目录项没有副本可证，所以快速路径用大小+mtime 校验身份，安全性不打折。

**实测**（`tests/bench.py`，64 MB 文件）：

| | 耗时 | 快照占用 |
|---|---|---|
| 快速路径 | **55 ms** | **0 MB** |
| 旧方式 | 517 ms | 67 MB |

**测试**：`test_safestore.py::MoveTests`（6 项，含跨卷回退、目标已存在被拒、逐字节可逆）

---

### 2. 横向/纵向筛选全量解码图片 ✅

**原状**：`full_app.py:249` 用 `reader.read()` 完整解码每张图，只为比宽高。

**改法**：`core/scanner.py` 只在需要时调 `metadata.read()`，后者用 Pillow 的 `Image.open` 读文件头（不 `load()`），或直接读 EXIF 的 `PixelXDimension`。

**实测**（40 张 6000×4000）：

| | 耗时 |
|---|---|
| 只读文件头 | **2.8 ms** |
| 旧的全量解码 | 2 958 ms |

**约 1000×**。1 200 张小图的对照也在基准里（83 ms vs 135 ms）。

---

### 3. 每次操作都是模态阻塞 ✅

**原状**：`_work()` 用嵌套 `QEventLoop` + 禁用 `centralWidget`，每按一次数字键界面冻结到复制校验完成。

**改法**：`core/opqueue.py` 单工后台队列。按键后 `_detach()` 立即把该项移出队列、翻到下一张，文件操作排在后台串行执行。失败会按原索引回插并红字提示，可重试。单工是刻意的——事务引擎同时只允许一个 journal。可在 **设置 → 性能** 关掉，回到同步模式。

**测试**：`test_scanner_engine.py::QueueTests`（顺序、失败保留与重试、一个失败不阻塞其余、事件上报）

---

### 4. 快照永不自动清理，且只能全清 ✅

**原状**：`备份管理` 只有一个「清空全部历史和备份」，连撤销记录一起没了。

**改法**：`QuotaPolicy(max_operations, max_bytes, max_days)` 三档上限，`reclaim_candidates()` 从最旧开始退役，删掉恢复副本并把记录标为不可撤销（`state.retire()`）——**不动任何用户文件**。每次操作后有一道廉价闸门检查是否超限。状态栏常驻配额条，**设置 → 安全与备份** 可调三档。

**测试**：`test_safestore.py::QuotaTests`（6 项，按天/按次/按字节各一，含边界不触发）

---

## 二、P1 · 长期会变慢 / 有隐患

### 5. `state.json` 全量读写 + 每次 deepcopy ✅

**改法**：换成 SQLite（`core/state.py`）。所有变更走一个 `delta` 字典，在**单次事务**里应用；delta 随 journal 一起落盘，所以文件和状态要么都成、要么都不成。delta 是幂等的，崩溃恢复重放不会重复计数。

**实测**：

| 库中已有记录 | 插入一条耗时 |
|---|---|
| 0 | 0.41 ms |
| 5 001 | 0.60 ms |
| 25 002 | 1.72 ms |

**测试**：`test_state_ops.py::StateTests`（12 项，含幂等重放、3000 条后插入耗时上限断言）

---

### 6. 查重结果无持久化缓存 ✅

**改法**：`core/hashcache.py`，SQLite，键为 `(路径, 大小, mtime_ns)`，存 SHA-256 / pHash / dHash / 清晰度。文件改了自动失效重算。

**实测**（1 200 张）：精确重复 542 ms → **49 ms**；相似图片 3 258 ms → **256 ms**。

---

### 7. 回收站操作产生两份完整副本 ✅

**原状**：先留快照，再把快照复制一份成 `回收-xxx-原名` 送系统回收站——大视频瞬间双倍占用，而且从系统回收站还原出来的是带前缀的副本，不回原路径。`send2trash` 失败时那份副本还会永久滞留且不计入占用统计。

**改法**：两种模式二选一（**设置 → 安全与备份**）：
- **仅应用内软删除**（默认）：一份恢复副本，`Ctrl+Z` 原地还原。
- **送系统回收站**：`send2trash` 直接处理**原文件**，不再复制。这类记录标记为不可应用内撤销，界面如实显示。

`send2trash` 缺失时抛 `TrashUnavailable` 而不是静默吞掉（旧版会吞）。

---

### 8. `app_v2.py` 里约 700 行死代码 ✅

**改法**：整体重写为分层包。`core/` 零 Qt 依赖、全部有测试；`ui/` 只画不想。旧的 `OperationRecord`、`_record_operation`、旧 undo/redo、旧扫描筛选、`undo_backups` 目录全部不存在了。

`test_static.py` 静态保证这条分层不会退化：core 里出现任何 `PySide6` import 即测试失败。

---

## 三、P2 · 细节

| 问题 | 处理 |
|---|---|
| 无日志文件 | `core/logsetup.py` 按天滚动、默认留 7 天；**设置 → 关于 → 导出诊断包**（含日志与事务记录，**不含媒体文件**） |
| 无版本号 | `qingjian.__version__ = "2.0.0"`，`--version`、设置页、日志首行都有 |
| 重命名未校验 Windows 保留名 | `core/naming.py` 挡 `CON/PRN/AUX/NUL/COM1-9/LPT1-9`（含上标数字变体）、尾部点与空格、路径长度 |
| 两套元数据栈（hachoir + PyAV） | **去掉 hachoir**。自写 `core/isobmff.py` 直接读 `moov/mvhd` 拿时长尺寸；PyAV 在装了的时候优先，没装也能用 |
| `reveal_current` 未捕获异常 | `core/platform_.py::reveal()` 返回 bool，三平台各自实现 |
| 清理备份后 `_session_ids` 未重置 | `engine.clear_backups()` 一并清空 |
| 纯 Windows 绑定 | `core/platform_.py` 抽象了 reveal / trash / 卷标识 / 大小写敏感探测，core 层可在 Linux 跑（222 项测试就是在 Linux 上跑的） |

---

## 四、新功能

### A · 效率手感

| # | 功能 | 位置 |
|---|---|---|
| A1 | **缩略图网格 + 多选批量分类** | `ui/browsers.py::ThumbnailGrid`，`G` 切换，选中后按 `1–0` 整批处理 |
| A1 | **底部胶片条** | `ui/browsers.py::Filmstrip`，单张模式下可视化跳转 |
| A2 | **后台队列 + 立即翻页** | 见 P0-3 |
| A3 | **目标目录按日期自动分层** | `core/template.py`，`{YYYY}/{YYYY-MM}` |
| A4 | **文件命名模板** | 同上，`{YYYY-MM-DD}_{seq:4}_{name}`，序号跨会话延续 |
| A5 | **评分与色标** | `Shift+1–5` / `Alt+1–5`，不移动文件；缩略图上直接显示；可按评分筛选和排序 |
| A6 | **目标文件夹搜索** | 侧栏 `/` 或 `Ctrl+F`，回车执行第一个匹配 |
| A8 | **会话续做** | 记住上次源目录、筛选、排序和看到第几张 |

### B · 照片整理的领域能力

| # | 功能 | 位置 |
|---|---|---|
| B1 | **RAW 支持** | 27 种扩展名；`core/exifread.py` 自读 TIFF/EXIF 拿拍摄时间与机身镜头；`core/imaging.py` 提取内嵌 JPEG 预览，无需 RAW 解码器 |
| B2 | **伴随文件联动** | `core/sidecar.py` + `core/ops.py`，同事务、同改名、整组撤销。**这是原版最容易造成实际数据混乱的缺口** |
| B3 | **相似图检测 + 连拍分组** | `core/dedupe.py`，32×32 DCT 感知哈希 + 8 段分桶索引（避免 O(n²)），连拍按清晰度择优 |
| B4 | **模糊 / 过曝 / 黑屏标记** | `core/imaging.py::quality_score()`，拉普拉斯方差 + 直方图 |
| B5 | **动图播放** | `ui/preview.py` 用 `QMovie`，GIF/APNG/动态 WebP 真的动 |
| B6 | **跨目录查重** | 递归扫描下天然覆盖；忽略清单按源目录隔离 |
| B7 | **更多筛选** | 仅 RAW、方形、动图、已评分、未评分、有色标、文件名正则、日期区间、分辨率区间 |

### C · 安全与性能

C1 同卷快路径、C2 配额分级回收、C3 快速校验模式、C4 诊断包、C6 —— 均已完成（见上）。
C5（移动硬盘断连提示）由事务引擎的身份校验兜底：目标不可写时报明确错误并保持原状。

### D · 工程与分发

D1 分层重写、D2 版本号、D3 便携模式（`qingjian-portable` 目录或 `--data-dir`）、
D4 去 hachoir + spec 里排除十几个用不上的 Qt 模块、D6 平台抽象 —— 完成。
**D5 浅色主题：没做。** 看图选片本来就该用深色环境，加浅色是退步，这条我替你砍了。

---

## 五、验证情况

```
python run_tests.py       222 项通过
ruff check                全部通过
python -m compileall      全部通过
python tests/bench.py     见上表
```

**未在此环境验证的部分**：Qt 界面层。这台沙箱的出口白名单挡住 PyPI，装不上 PySide6。
应对是把逻辑全部搬出 Qt——`core/` 零 Qt 依赖、222 项测试全在这层；`ui/` 只剩绘制与转发，
且只使用你原有 4245 行里已经跑通的 Qt API。

另外用静态检查补了界面层（`tests/test_static.py`，12 项）：

- core 里出现任何 `PySide6` import → 失败
- 界面调用的每一个 `self.engine.X()` 在 `Engine` 上不存在 → 失败
- 界面读的每一个 `self.settings.X` 不是 `Settings` 字段 → 失败
- 每一个 `tr("键")` 不在目录里 → 失败
- `tr()` 少传占位符参数 → 失败
- 英文栏残留中文 → 失败
- 类里读了从没赋值的属性 → 失败

**请在 Windows 上跑一次**：

```
MediaSorter.exe --selfcheck
```

它会离屏构造真实窗口，切语言、切视图、遍历所有筛选排序、打开每个对话框、
分类一次再撤销、截图，然后逐项报 PASS/FAIL。只碰临时目录。
