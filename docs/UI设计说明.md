# 轻拣 2.0.4 UI 基线说明

2.0.4 按用户要求回到 2.0.3 已测试版的深色/紫色 UI 基线。本版本不做视觉改版，仅修复启动状态和恢复按钮的功能性问题；既有 Qt 组件结构与交互保持不变。

## 设计方向

- **定位**：面向长时间操作的图片/视频整理工作台，预览区域优先，快捷键操作为主。
- **色彩**：深色底色承载内容，紫色用于主操作、当前选择和键位提示，青绿色用于成功/队列状态。
- **层级**：窗口标题区 → 工具条 → 预览/缩略图 → 绑定卡片与状态条，使用少量边界线而非大量装饰。
- **交互**：保留键盘优先流程；按钮、输入框、组合框和复选框都提供可见焦点态；点击目标保持足够尺寸和间距。
- **状态**：已配置、当前项、悬停、禁用、成功、警告、错误分别使用不同的表面和语义颜色，避免只依赖数字或颜色判断。

## 本次改动

- 保留 2.0.3 已测试版的主题、布局、控件和间距。
- 启动时按当前来源上下文初始化撤销/恢复按钮，避免残留日志让“恢复上一步”误显示为可用。
- 保持现有快捷键、撤销/恢复、查重、视频预览和多语言逻辑不变。
